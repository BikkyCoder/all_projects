let imgRun = document.querySelector('img')

let Ani_image = animation = 0

setTimeout(function(){
    Ani_image.style.transform = rotate =+ 100
},10)





























// let wheel = document.getElementById('wheel');
// let rotationDegree = 0;
// let speed = 100; // Initial speed in ms, lower means faster rotation
// let increaseSpeedInterval = 1000; // Speed will increase every second

// function rotateWheel() {
//   // Increment the rotation degree
//   rotationDegree += 10;
  
//   // Apply the rotation using CSS
//   wheel.style.transform = `rotate(${rotationDegree}deg)`;
  
//   // Increase the speed (decrease the interval time)
//   speed = Math.max(10, speed - 10); // Set a minimum limit to prevent it from being too fast
  
//   // Call the function again after `speed` ms
//   setTimeout(rotateWheel, speed);
// }

// // Start rotating the wheel
// setTimeout(rotateWheel, speed);

// // Increase speed every second
// setInterval(() => {
//   console.log('Increasing speed...');
// }, increaseSpeedInterval);